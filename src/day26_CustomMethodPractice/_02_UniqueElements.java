package day26_CustomMethodPractice;import utility.ArraysUtility;import java.util.Arrays;public class _02_UniqueElements {    public static void main(String[] args) {        int[] array = {1, 1, 2, 3, 3, 4, 5, 5, 6, 7, 7, 7};        int[] unique = uniqueElements(array);        System.out.println(Arrays.toString(unique));  // [2, 4, 6]        double[] array2 = {1.5, 1.5, 2.5, 3.5, 4.5, 5.5, 5.5, 4.5};        double[] unique2 = uniqueElements(array2);        System.out.println(Arrays.toString(unique2));  // [2.5, 3.5]    }    // Returns do unique elements of the array as a New array    public static int[] uniqueElements(int[] array) {        int[] result = {};    // new int [0] it is temporary        for (int each : array) {            if (ArraysUtility.frequencyOfElement(array, each) == 1) {   //if freq is 1 , means the element is unique                result = ArraysUtility.addElement(result, each);            }        }        return result;   // result is new array    }    public static double[] uniqueElements(double[] array) {        double[] result = {};    // new int [0] it is temporary        for (double each : array) {            if (ArraysUtility.frequencyOfElement(array, each) == 1) {   //if freq is 1 , means the element is unique                result = ArraysUtility.addElement(result, each);   // new array created and every add element            }        }        return result;   // result is new array    }    public static char[] uniqueElements(char[] array) {        char[] result = {};    // new int [0] it is temporary        for (char each : array) {            if (ArraysUtility.frequencyOfElement(array, each) == 1) {   //if freq is 1 , means the element is unique                result = ArraysUtility.addElement(result, each);   // new array created and every add element            }        }        return result;   // result is new array    }    public static String[] uniqueElements(String[] array) {        String[] result = {};    // new int [0] it is temporary        for (String each : array) {            if (ArraysUtility.frequencyOfElement(array, each) == 1) {   //if freq is 1 , means the element is unique                result = ArraysUtility.addElement(result, each);   // new array created and every add element            }        }        return result;   // result is new array    }}
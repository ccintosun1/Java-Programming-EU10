package day29_ArrayList;import java.util.ArrayList;import java.util.Arrays;import java.util.Collections;public class _12_NthLargestNumber {    public static void main(String[] args) {        ArrayList<Integer> list = new ArrayList<>();        list.addAll(Arrays.asList(1, 2, 3, 4, 5, 6, 7, 7, 8, 8));        list.removeIf(p -> Collections.max(list) == p);   // 8 leri siler max lari.        list.removeIf(p -> Collections.max(list) == p);   // 7 leri  leri siler max lari.        int max = Collections.max(list);        System.out.println(max);  // 6        System.out.println("------ removeIf i for ici nde de kullanabilirsin----------------------");        int n = 3;        for (int i = 0; i < n; i++) {            list.removeIf(p -> Collections.max(list) == p ); // Burada da son 3 buyuk rakamdan kacar adet varsa siler.        }        int max2 = Collections.max(list);        System.out.println("max2 = " + max2);  // 3    }}/*1. write a program that can return the nth largest number from an arraylist			arraylist = {1,2,3,4,5,6,7, 7 ,8, 8}			n = 5			output:				4 */
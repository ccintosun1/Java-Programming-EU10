package day21_ArraysUtility_For_EachLoop;import java.util.Scanner;public class _06_AvarageNumber_ForEachLoop {    public static void main(String[] args) {        //------------------- FIND AVARAGE NUMBER WITH NORMAL FOR LOOP ------------------//        int[] numbers = {10, 30, 45, 60, 23, 67};        double sum = 0;        for (int i = 0; i < numbers.length; i++) {            sum += numbers[i];        }        double average = sum / numbers.length;        System.out.println("average = " + average);  // 39.16666666...        //------------------- FIND AVERAGE NUMBER WITH FOR EACH LOOP ------------------//        double sum2 =0;        double average2 =0;        for (int each : numbers) {            sum2 += each;      // sum i for un icinde tanimlamamalisin.        }            average2 = (sum2 / numbers.length);        System.out.println("average2 = " + average2); // 39.16666666.../*        Scanner scan = new Scanner(System.in);        //----------- take length from the user -----------//        System.out.println("How many numbers they want to enter");        byte length = scan.nextByte();        //------ determine the length of Array from user and decleare int total ----//        int[] numbers = new int[length];        int sum = 0, avarage = 0;        //------------- Enter the numbers Length times from the user -------------------//        for (int i = 0; i < numbers.length; i++) {            System.out.println("Enter a number (remaining try " + (numbers.length - i) + ")");            numbers[i] = scan.nextInt();            sum += numbers[i];  // calculate sum        }        //------------------calculate the avarage of the numbers and print it----------------        avarage = sum / numbers.length;        System.out.println("avarage = " + avarage);        	4. AverageNumber:			1. Ask the user how many numbers they want to enter			2. get all the inputs from the user and store them in an array            3. Iterate the array & return the average number         */    }}
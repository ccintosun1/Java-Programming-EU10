package day21_ArraysUtility_For_EachLoop;import java.util.Arrays;public class _13_ReverseArray {    public static void main(String[] args) {        //-------------- TASK :  reverse write Arrays --------------------//        int[] numbers = {10, 20, 30, 40};        // index numbers: 0   1   2   3        int[] reversed = new int[numbers.length];    // to make sure that array has enough capacity to contain all the elements of first        System.out.println(Arrays.toString(reversed)); // [0, 0, 0, 0, 0]        for (int i = numbers.length - 1, j = 0; i >= 0; i--, j++) {            reversed[j] += numbers[i];        }        System.out.println(Arrays.toString(reversed));  // [40, 30, 20, 10]    }}
package day34_GarbageCollection_AccesModifiers;import day13_String.StringIntro;import java.util.ArrayList;public class _02_Circle {    public double radius;    // instance variable    public static double pi = 3.14;   // static variable  You can declare like that because this is one step if you need                                      // more than one step you need to do step in the static block                                      // Burda initialize yaptigini static block da da initialize yapabilirsin. Sorun yok.    // Bilmelisin ki static ler tek assign ile bitmez o sebeple static block kullanicaz.    // MAke sure we need to use static block to SET static variable easly.    public static String name;    public static ArrayList<Integer> numbers;    // numbers.add(10);  // compile error because you can add 10 to ArrayList into the static block                         // because you need more than one steps    // public static Sheet sheet; in the future we will use different type of static variables.                                  // Hep double String int kullanmicaz.                                  // we will get excell file like this later.    // We will use more than one steps to get infos from the excellFille    // 1- Get the file 2- read the file 3- get the sheet like this    // Bu sebeple assign etmiyoruz. Static block icinde diger adimlar ile bu islemi yapiyoruz.    public _02_Circle(double radius) {  // object create etmeden constructer a ulasamazsin.        this.radius = radius;          // Bu sebeple constructer icinde static parameter variable create edilemez.        // pi = 3.14; // Her zaman 3.14 olucaktir.  // Boyle kullanmiyoruz.        /*        NOTE : Ustte 1 instance ve bir static variable create ettik. SOnra constructor create ettik.        Constructor icinde static parameter create etmedik. Cunku constructor can run multiple times        But static variable can only run one times, Because that we declared pi = 3.14 in the constructor        That`s mean for example five time we declared and initialised 5 time the value in the constructor.        when you call the constructor Pi = 3.14 olacak over and over again.         */    }    static {            // Yukarida create ettigin variable leri static block icinde initialize etmelisin.                        // Tek satir sa da burda yapmalisin. Yukarda da yapabilirsin ama birden fala cagirmalarda problem olur.                        // Ki genelde instance kullanicaz.        // radius = 5;  // compile error verir static icinde instance kullanilamaz.        pi = 3.14;        name = "Circle";        numbers = new ArrayList<>();        numbers.add(10);        numbers.add(20);        // Baska bir class da tum bu assign ettigin variable ve digerlerini kullanabilirsin.        // just executed one time the other class    }    /*    public static void main(String[] args) {  // static block da yaptigin her seyi main method icinde de yapabilirsin.        pi =3.14;                             // Fakat Baska class da kullanmak istersen orda main method icinde assign edemezsin.        name = "Circle";                      // main method sadece ayni class da kullanilir. Baska bir class da assign lar olmaz.        numbers = new ArrayList<>();          // null olurlar        numbers.add(10);        numbers.add(20);        System.out.println("pi = " + pi);              // pi = 3.14        System.out.println("name = " + name);          // name = Circle        System.out.println("numbers = " + numbers);    // numbers = [10, 20]    }     */}
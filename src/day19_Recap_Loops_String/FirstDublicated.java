package day19_Recap_Loops_String;public class FirstDublicated {    public static void main(String[] args) {        String str = "abccddefgh";        String result = "";        int count = 0;        for (int i = 0; i < str.length(); i++) {            for (int j = i + 1; j < str.length(); j++) {                String dub = "" + str.charAt(i);  // a                String ch = "" + str.charAt(j);  //                if (dub.equals(ch)) {                    count++;                }                if (count == 1) {                    result = dub;                    break;                }            }        }        int indexofFirstDublicate = str.indexOf(result);        System.out.println("first dublicate character is " + result+ " and index number of "+ indexofFirstDublicate);        /*        1. Write a program that can return the first duplicated character from a string         */    }}
package day29_ArrayList;import java.util.ArrayList;import java.util.Arrays;public class _10_ArrayListPractice2 {    public static void main(String[] args) {        ArrayList<String> employees = new ArrayList<>();        employees.addAll(Arrays.asList("Ali", "David", "Ahmet", "Jimmy", "Daniel", "aaron", "Ahmed", "David", "Shay" ));        // Every Collections Bulk Operations lar toplu listelerde Arrays.asList ile yazilir        employees.retainAll(Arrays.asList( "Ahmet", "David"));  // remove unmatching elements from the ArrayList        System.out.println(employees);  // [David, Ahmet, David]        System.out.println("--------Bir string[ ] array deki M ile baslayan elementler nasil silinir ----------");        String[] names = {"Marry", "Monica" , "Mehray" , "Musti" , "Sumeyra" , "Hasan" , "Beril"};        // Now we will remove startsWith upperCase M        // 1. We must convert Array to ArrayList        ArrayList<String> list = new ArrayList<>(Arrays.asList(names));        // 2. removes startsWith M        list.removeIf(p -> p.startsWith("M"));  // or list.removeIf(p -> p.charAt(0) == 'M' );        // convert to Array again        names = list.toArray(new String[0]);        // print the Array        System.out.println(Arrays.toString(names));  // [Sumeyra, Hasan, Beril]    }}
package day20_Arrays;public class AppearedTwice {    public static void main(String[] args) {        char[] chars = {'A', 'A', 'A', 'B', 'C', 'C', 'D', 'D'};        for (int i = 0; i < chars.length; i++) {            char ch = chars[i];            int freq = 0;            for (int k = 0; k < chars.length; k++) {                if (ch == chars[k]){                    freq++;                }            }            if (freq == 2){                System.out.print(ch);  // CCDD            }                    }    }}
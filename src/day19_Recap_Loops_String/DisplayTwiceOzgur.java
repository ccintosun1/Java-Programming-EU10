package day19_Recap_Loops_String;import java.util.Scanner;public class DisplayTwiceOzgur {    public static void main(String[] args) {        Scanner input = new Scanner(System.in);        //System.out.println("Enter a String");        // String str = input.nextLine();        String str = "aaaabbccccccddeeeeee";  // when this line deactivated and upper line activated str can be taken from user        String result = "";        for (int i = 0; i < str.length(); i++) {  // Outer loop is used to separate every ch            char ch = str.charAt(i);            int freq = 0;            for (int j = 0; j < str.length(); j++) { // Inner loop is used to find the frequency                char each = str.charAt(j);                if (ch == each){                    freq++;                }            } // Inner loop closing//  When we built the if statement like this ==> result = bd            if (freq==2){                if (result.contains(""+ch)){                    continue;                }                result += ch;            }//  When we built the if statement like this ==> result = result = bbdd            //     if (freq==2){            //        result += ch;            //    }        } // Outer loop closing        System.out.println("result = " + result);    }}/*3. Write a program that can display all the characters that appeared twice in the string.        String str = "aaaabbccccccddeeeeee";            result = bd; */
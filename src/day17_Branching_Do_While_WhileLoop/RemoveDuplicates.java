package day17_Branching_Do_While_WhileLoop;public class RemoveDuplicates {    public static void main(String[] args) {        String str = "AABBCC";        String result = ""; //ABC        for (int i = 0; i < str.length(); i++) {            String ch = "" + str.charAt(i);   // "A", "A", "B", "B", "C", "C"               // yada char ch = str.charAt(i)            if (result.contains(ch)) {    // contains method only gives us String data Type // if(result.contains(""+ch)) yapmalisin                continue; // skips            }            result += ch;        }        System.out.println(result);  // ABC    }}
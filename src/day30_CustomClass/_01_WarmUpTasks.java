package day30_CustomClass;import java.lang.reflect.Array;import java.util.ArrayList;import java.util.Arrays;import java.util.Collections;public class _01_WarmUpTasks {    public static void main(String[] args) {  //1. write a program that can swap the first and last elements of an ArrayList        ArrayList<Integer> number = new ArrayList<>();        number.addAll(Arrays.asList(1,2,3,4,5,6,7,8));        Collections.swap(number,0,number.size()-1);        System.out.println(number);        System.out.println("---------------------------------------------------------");    /* 2. Write a program that can move all the zeros to the last indexes of ArrayList / 0 lari sona atma programi        Ex:      list: {1,0,2,0,3, 0,4, 0}        output: [1, 2, 3, 4, 0, 0, 0, 0]      */        ArrayList<Integer> list = new ArrayList<>();        list.addAll(Arrays.asList(1,0,2,0,3,0,4,0));        System.out.println(list); // [1, 0, 2, 0, 3, 0, 4, 0]        int size  = list.size();  // 8        list.removeAll(Arrays.asList(0));        System.out.println(list); // [1, 2, 3, 4]        int newSize = list.size();        int totalNumberOfZero = size - newSize;        System.out.println("totalNumberOfZero = " + totalNumberOfZero);  // 4        for (int i = 0; i < totalNumberOfZero; i++) {            list.add(0);        }        System.out.println(list);   // [1, 2, 3, 4, 0, 0, 0, 0]        System.out.println("----  new technic to same question above  ------------- ");        ArrayList<Integer> list2  = new ArrayList<>();        list2.addAll(Arrays.asList(1, 0, 2, 0, 3, 0, 4, 0));        ArrayList<Integer> result = new ArrayList<>();        int freqOfZero = Collections.frequency(list2,0);        for (Integer each : list2) {            if (each != 0){                result.add(each);            }        }        for (int i = 0;  i< freqOfZero ; i++) {            result.add(0);        }        System.out.println(result); // [1, 2, 3, 4, 0, 0, 0, 0]        System.out.println("----------------------------------------------------");       /* 3. Write a program that can extract the special characters,       digits and letters from a string and stores them into separate        ArravLists of Characters        Ex:        str = "ABCD123$%#@&456EFG!"        output:        list1 : {1, 2, 3, 4, 5, 6}        list2 : {A, B, C, D, E, F, G}        List3 : {$, %, #, @, &, !}         */        System.out.println("----------- The Best solution --------------");        String str =  "ABCD123$%#@&456EFG!";        // Yeni bir Character ArrayList create ettim        ArrayList<Character> chars = new ArrayList<>();        // str2 deki String elemanlari ArrayListe ekledim        for (int i = 0; i < str.length() ; i++) {            chars.add( str.charAt(i));        }        // REMOVE IF ILE DIGIT OLMAYANLARI SILIYORUM        ArrayList<Character> digits = new ArrayList<>(chars);   // chars ArrayListini copyaladim icinden silicem        digits.removeIf( p -> !Character.isDigit(p));        // REMOVE IF ILE letter OLMAYANLARI SILIYORUM        ArrayList<Character> letters = new ArrayList<>(chars);   // chars ArrayListini copyaladim icinden silicem        letters.removeIf( p -> !Character.isLetter(p));        // REMOVE IF ILE Special Character OLMAYANLARI SILIYORUM        ArrayList<Character> specialChar = new ArrayList<>(chars);      // chars ArrayListini copyaladim icinden silicem        specialChar.removeIf( p -> Character.isLetterOrDigit(p));        /* VEYA BOYLE DE YAPABILIRSIN        specialChar.removeAll( letters );        specialChar.removeAll( digits );        */        System.out.println("digits2 = " + digits);        System.out.println("letters2 = " + letters);        System.out.println("specialChar2 = " + specialChar);    }}
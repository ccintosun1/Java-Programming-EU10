package day31_Constructors;public class _02_BankAccount {    public String accountHolder;    public long accountNumber;    public double balance;    // instance methods ( static icermeyen methodlara instance method denir )    public void setInfo(String accountHolder, long accountNumber) {        this.accountHolder = accountHolder;        this.accountNumber = accountNumber;    }    public String toString() {        return "_02_BankAccount{" +                "accountHolder='" + accountHolder + '\'' +                ", accountNumber=" + accountNumber +                ", balance= $" + balance +                '}';    }    public void checkBalance() {        System.out.println("Your available balance is: $" + balance);    }    public void deposit(double amounth) {        if (amounth <= 0) {            System.err.println("depositing amount can not be zero or negative");            return;  // exit the method        }        balance = +amounth;    }    public void withdraw(double amounth) {        if (amounth > balance) {   // if withdrawing amount is grader than available balance            System.err.println("Insufficient Balance");            return; // exit the loop        }        if (amounth <= 0) {  // if withdrawing amount is negative or zero            System.err.println("Withdrawing amount can not be negative or zero");        }        balance -= amounth;    }}/*	BankAccount Task:	        Attributes:	                1. accountHolder, 2. accountNumber, 3. balance	        Actions:	        	1. setInfo(): sets the accountHolder and accountNumber attributes	        	2. toString()	            3. checkBalance():displays the available balance	            2. deposit(): increases the balance by the given amount				3. withdraw(): decreases the balance by the given amount */